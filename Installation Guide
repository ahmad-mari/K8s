Before we start here, you will gfind a file called k8s-installation-on-all-nodes.sh, clone it on all nodes and run it, after that we can start with the following.
#run the Following on master node only 
sudo kubeadm init

#After this it will result a kubeadm join code paste it on WORKERS nodes only
EX: kubeadm join 164.90.215.77:6443 --token t7wxy0.gk8pjte1t47252dg \
        --discovery-token-ca-cert-hash sha256:d2dcc8e2edd629f865247f7c0924d20abdfa6ae60dbb9e95dd8430a27e0ee3b3

#Now run this on master only 

mkdir -p $HOME/.kube; sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config; sudo chown $(id -u):$(id -g) $HOME/.kube/config
kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.25.0/manifests/calico.yaml
